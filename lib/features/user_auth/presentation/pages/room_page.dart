import 'package:flutter/material.dart';

class PinjamRuanganPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Pinjam Ruangan'),
      ),
      body: Center(
        child: Text('Ini Halaman Pinjam Ruangan'),
      ),
    );
  }
}
